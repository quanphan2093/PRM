<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".Project.OrderCustomerActivity"
            android:exported="false" />
        <activity
            android:name=".Project.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Project.OrderActivity"
            android:exported="false" />
        <activity
            android:name=".Project.CartActivity"
            android:exported="false" />
        <activity
            android:name=".Project.ProductDetailActivity"
            android:exported="false" />
        <activity
            android:name=".Project.ProductActivity"
            android:exported="true">
        </activity>
        <meta-data
            android:name="com.google.android.gms.auth.api.signin"
            android:value="@string/default_web_client_id" />
    </application>

</manifest>